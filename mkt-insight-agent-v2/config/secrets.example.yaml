# config/secrets.example.yaml — template. Copy to secrets.yaml and fill real values.
# secrets.yaml is NOT committed (in .gitignore).

admin_token: "change-me-to-a-random-string"
db_password_ro: "change-me"
